(function() {
	// body...
	'use strict';
	
	/**
	* module.main Module
	*
	* Description
	*/
	angular.module('module.main', []);
		// .run(['$rootScope', function($rootScope) {
		// 	// Initialize $rootScope.logged_user with an empty object if it's not already initialized
		// 	if (!$rootScope.logged_user) {
		// 		$rootScope.logged_user = {};
		// 	}
		// }]);
	
})();